# Known issues

## Open

- Corrupted `node_modules/` left over from interrupted Cowork-sandbox `npm install` attempts. Resolution: Ben deletes from PowerShell and re-runs `npm install` from his own terminal. See `CLAUDE.md` lesson BIPS-L1.

## Resolved

(nothing yet)
